<Window x:Class="DeviceMonitorUi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:infra="clr-namespace:DeviceMonitorUi.Infrastructure"
        xmlns:vm="clr-namespace:DeviceMonitorUi.ViewModels"
        Title="Device Monitor"
        Width="1000"
        Height="600"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <infra:KindToBrushConverter x:Key="KindToBrush" />
        <infra:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis" />
        <infra:IconColorConverter x:Key="IconColorConv" />

        <Style x:Key="IconText" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Width" Value="24" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <DataTemplate x:Key="ChildEventTemplate" DataType="{x:Type vm:EventViewModel}">
            <Border Padding="32,4,8,4" BorderBrush="#333" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{Binding Icon}"
                               Style="{StaticResource IconText}"
                               Foreground="{Binding Kind, Converter={StaticResource KindToBrush}}" />
                    <TextBlock Grid.Column="1" Text="{Binding Timestamp}" Foreground="#CCC" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="2" Text="{Binding Name}" Foreground="White" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="3"
                               Text="{Binding DeviceId}"
                               Foreground="#999"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Grid.Column="4" Text="{Binding Manufacturer}" Foreground="#AAA" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="5"
                               Text="{Binding Description}"
                               Foreground="#AAA"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="SingleEventTemplate" DataType="{x:Type vm:ClusterViewModel}">
            <Border Padding="8,6" BorderBrush="#333" BorderThickness="0,0,0,1" Background="#252525">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{Binding Icon}"
                               Style="{StaticResource IconText}"
                               Foreground="{Binding IconColor, Converter={StaticResource IconColorConv}}" />
                    <TextBlock Grid.Column="1" Text="{Binding Timestamp}" Foreground="#CCC" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="2" Text="{Binding SingleName}" Foreground="White" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="3"
                               Text="{Binding SingleDeviceId}"
                               Foreground="#999"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Grid.Column="4" Text="{Binding SingleManufacturer}" Foreground="#AAA" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="5"
                               Text="{Binding SingleDescription}"
                               Foreground="#AAA"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="MultiEventTemplate" DataType="{x:Type vm:ClusterViewModel}">
            <Border BorderBrush="#333" BorderThickness="0,0,0,1" Background="#252525">
                <Expander IsExpanded="False">
                    <Expander.Header>
                        <Grid Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Expander}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Icon}"
                                       Style="{StaticResource IconText}"
                                       Foreground="{Binding IconColor, Converter={StaticResource IconColorConv}}" />
                            <TextBlock Grid.Column="1" Text="{Binding Timestamp}" Foreground="#CCC" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{Binding TotalChanges}" Foreground="#AAA" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="3" Text="{Binding Breakdown}" Foreground="#AAA" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="4" Text="{Binding SummaryLabel}" Foreground="White" VerticalAlignment="Center" />
                        </Grid>
                    </Expander.Header>
                    <ItemsControl ItemsSource="{Binding Children}" ItemTemplate="{StaticResource ChildEventTemplate}" />
                </Expander>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D2D" Padding="16,10">
            <DockPanel>
                <TextBlock Text="Device Monitor"
                           Foreground="White"
                           FontSize="18"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />
                <Button DockPanel.Dock="Right"
                        Content="Clear"
                        Command="{Binding ClearCommand}"
                        Padding="16,6"
                        FontSize="13"
                        Background="#444"
                        Foreground="White"
                        BorderBrush="#666"
                        Cursor="Hand" />
            </DockPanel>
        </Border>

        <StackPanel Grid.Row="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Monitoring device changes..."
                       Foreground="#CCC"
                       FontSize="28"
                       FontWeight="Light"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Plug in or remove a device to see events here."
                       Foreground="#777"
                       FontSize="14"
                       Margin="0,8,0,0"
                       HorizontalAlignment="Center" />
        </StackPanel>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding IsEmpty, Converter={StaticResource InverseBoolToVis}}">
            <ItemsControl ItemsSource="{Binding Clusters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ClusterViewModel}">
                        <ContentControl Content="{Binding}">
                            <ContentControl.Style>
                                <Style TargetType="ContentControl">
                                    <Setter Property="ContentTemplate" Value="{StaticResource MultiEventTemplate}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSingleEvent}" Value="True">
                                            <Setter Property="ContentTemplate" Value="{StaticResource SingleEventTemplate}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ContentControl.Style>
                        </ContentControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
